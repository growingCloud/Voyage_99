# Chapter09_23. 큐를 이용한 스택 구현 (255p)
# 난이도 : ★
# Leet code Num. : 225

# 큐를 이용해서 다음 연산을 지원하는 스택을 구현하라.
# push(x) : 요소 x를 스텍에 삽입한다.
# pop() : 스텍의 첫 번째 요소를 삭제한다.
# top() : 스택의 첫 번째 요소를 가져온다.
# empty() : 스택이 비어 있는지 여부를 리턴한다.

# Mystack stack = new Mystack();
# stack.push(1);
# stack.push(2);
# stack.top();  // 2 리턴
# stack.pop();  // 2 리턴
# stack.empty();    // false 리턴